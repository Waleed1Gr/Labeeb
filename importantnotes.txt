# Revised Labeeb Project Tasks (Prioritized)

---

## 1. Foundation and Infrastructure Issues

### 1.1 Fix YOLO Model Loading
**Status**: Critical issue preventing startup  
**Location**: `detector.py`  
**Problem**: `torch.hub.load('ultralytics/yolov5su', 'yolov5su', pretrained=True)` points to non-existent repo  
**Solution**: Replace with local model loading via `YOLO('yolov5su.pt')` with ultralytics package  
**(DETECTOR IS NO LONGER NEEDED, SO THIS STEP IS IRRELEVANT)**

### 1.2 Fix `session_active` Variable 
**Comment**: `# * session active is not actually defined, FIXME: define the session_active variable in the config module.`  
**Location**: main.py, line ~82  
**Impact**: Critical for conversation state management  
**(FIXED BY USING THE GLOBAL "session_active" VARIABLE. THIS IS DONE RIGHT AFTER THE "def main()" LINE)**

### 1.3 Implement Proper Thread Handling
**Location**: Multiple files including `camera.py` and main.py  
**Problem**: Background threads for vision/audio need proper cleanup  
**(FIXED BY ADDING THE THREADING PACKAGE AND USING IT FOR VIDEO, IT WILL UPDATE LATER TO RECEIVE FROM CLIENT.)**

---

## 2. Server-Client Architecture Implementation (NEW PRIORITY)

### 2.1 Complete WebSocket Server Logic
**Location**: main.py, line ~30  
**Task**: Finalize WebSocket server implementation with proper threading  
**Comment**: `# ! this might have no use after implementing the server-client logic in the I/O system of labeeb.`

### 2.2 Implement Client-Side Audio/Video Streaming
**Location**: Needs new file pi_client.py  
**Task**: Create client to stream audio/video from Pi to server

### 2.3 Implement Audio Distribution Logic
**Location**: `speak.py`, lines ~16, ~30  
**Task**: Send generated audio to Pi for playback  
**Comment**: `# TODO: this function might be transferred to the PI, but if the server can just send the audio to the pi and play it immediately, then no need to transfer it.`

---

## 3. Audio System Improvements

### 3.1 Implement Barge-in System with API Streaming
**Location**: `speak.py`, line ~42  
**Task**: Use OpenAI API streaming to implement barge-in  
**Comment**: `# ! this is going to be replaced by a full barge-in system.`

### 3.2 Improve Audio Playback Detection
**Location**: main.py, line ~101  
**Task**: Implement WebRTC VAD for better speech detection  
**Comment**: `# TODO: replace this function, with the webrtcvad's method (is_playing) after implementing it.`

### 3.3 Remove Redundant Audio Functions
**Location**: `speak.py`, line ~47  
**Task**: Clean up audio-related functions after server-client implementation  
**Comment**: `# ! no need for this function, since the server can just send the audio to the pi and play it immediately.`

---

## 4. Vision System Improvements

### 4.1 Update YOLO Version
**Location**: `camera.py`, line ~24  
**Task**: Upgrade to YOLOv8 for better performance  
**Comment**: `#TODO: replace the yolo5 model with the yolo8 model.`

### 4.2 Fix Phone Detection in Server
**Location**: main.py, line ~50  
**Task**: Use `phone_person_detector` properly in server-client architecture  
**Comment**: `# FIXME: this should be replaced with phone_person_detector from camera.py`

---

## 5. Feature Implementation

### 5.1 Implement Delete Task Functionality
**Location**: main.py, line ~43  
**Task**: Add task deletion feature  
**Comment**: `# TODO: add the delete task functionality here.`

### 5.2 Relocate Function Classification
**Location**: `task_manager.py`, line ~74  
**Task**: Move `classify_input()` to appropriate module  
**Comment**: `# ? why is this here? it should be in the LLM file. FIXME: relocate this function to the LLM module.`

---

## 6. Future Localization (DEFERRED)

### 6.1 LLM Localization (Postponed)
**Location**: main.py, line ~121  
**Task**: Replace OpenAI API-based LLM with local implementation  
**Comment**: `# * this might be changed after making the LLM local.`  
**(POSTPONED: Will keep using API for Saudi dialect quality and streaming)**

### 6.2 TTS Localization (Postponed)
**Location**: `speak.py`, line ~11  
**Task**: Replace OpenAI TTS API with local TTS model  
**Comment**: `# TODO: fine-tune the TTS model to speak the saudi dialect.`  
**(POSTPONED: Will implement after core functionality is complete)**

### 6.3 STT Localization (Postponed)
**Location**: `listen.py`  
**Task**: Ensure Whisper runs locally for speech recognition  
**(POSTPONED: Will implement after core functionality is complete)**

---

## Implementation Strategy

Based on the revised priorities:

1. First, complete the server-client architecture
2. Implement streaming audio with barge-in support using API
3. Enhance vision system with YOLOv8
4. Add remaining features like task deletion
5. Later: Localize models incrementally starting with STT/TTS
6. Finally: Replace LLM with fine-tuned local model

This approach ensures you get a working product with Saudi dialect support while allowing for gradual localization.